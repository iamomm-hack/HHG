# Optional. Enables persistent image-backed X share pages.
BLOB_READ_WRITE_TOKEN=
# Public origin used in generated share URLs (for example https://your-app.vercel.app).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
